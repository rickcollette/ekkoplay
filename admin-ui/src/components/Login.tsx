import {FormEvent,useState} from 'react'
import {Album,LogIn} from 'lucide-react'
import {api,AdminSession} from '../lib/api'

export function Login({onLogin}:{onLogin:(session:AdminSession)=>void}){const [username,setUsername]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{onLogin(await api.login(username,password))}catch(x){setError(x instanceof Error?x.message:'Login failed')}finally{setBusy(false)}};return <div className="login-page"><form className="login-card" onSubmit={e=>void submit(e)}><div className="login-brand"><span><Album/></span><div><strong>ekkoPlayer</strong><small>ADMINISTRATION</small></div></div><h1>Welcome back</h1><p>Sign in to manage this player.</p><label>Username<input autoFocus autoComplete="username" value={username} onChange={e=>setUsername(e.target.value)}/></label><label>Password<input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="form-error" role="alert">{error}</p>}<button className="primary" disabled={busy||!username||!password}><LogIn/>{busy?'Signing in…':'Sign in'}</button></form></div>}
